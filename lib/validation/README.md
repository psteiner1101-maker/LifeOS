# lib/validation

Zod schemas shared by client and server (one per record type). The server
always re-validates independently of client-side validation. Empty until the
relevant feature phase begins.
