# tests/policies

Cross-workspace and cross-visibility access-blocking policy tests (both
release-blocking families, per amendment Part 8.7). These require a real
schema and RLS policies to exist, so no tests live here yet.
